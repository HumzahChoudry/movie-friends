<%= render 'lil_logo' %>
<%= render 'menu' %>
<!-- render 'comments' BROKEN -->

<div class='content'>
  <h1><%= @user.name %></h1>
  <%= image_tag(@user.picture_url, width: 600) if @user.picture.present? %>

  THIS SHOULD SHOW JUST COMMENTS I'VE MADE

</div>

<!-- stats on user -->
