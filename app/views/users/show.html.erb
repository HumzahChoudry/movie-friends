<%= render 'lil_logo' %>
<%= render 'menu' %>
<!-- render 'comments' BROKEN -->

<div class='content'>
  <h1><%= @user.name %></h1>
  <%= image_tag(@user.picture_url, width: 600) if @user.picture.present? %>


  <%= form_for @user do |f| %>
    <%= f.label :name, value: "EDIT USERNAME", class: 'session' %><br />
    <%= f.text_field :name, placeholder: "Name", class: 'session' %><br />
    <%= f.submit class: 'session'  %>
  <% end %>


</div>

<!-- stats on user -->
<div class='all_comments'>
  <%= plant_trees_for @comments %>
<div>
